#include <stdio.h>
/**
 * main - program shows the main function with int return type
 *
 * Return: the return has no arguments, 0
 *
 * The program aims to print out a script
 *
 * The command printf was used insted of puts
 */
int main(void)
{
	printf("with proper grammar, the outcome is a piece of art,\n");
	return (0);
}
