#include <stdio.h>

/**
 * main - program with main function with int return type
 *
 * Return: return has no arguments, 0
 *
 * The program aims to print out all the alphabet in lowercase
 */
int main(void)
{
	char sh;
	for (sh = 'a'; sh <= 'z'; sh++)
	{
		putchar (sh);
	} putchar (\n);
	return (0);
}
