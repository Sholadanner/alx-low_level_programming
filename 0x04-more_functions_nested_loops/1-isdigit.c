#include "main.j"

/**
 * _isdigit - has an int return type
 *
 * Return: the return value is to be calculated
 */

int _isdigit(int c)
{
	for (c = 0; c <= 9; c++)
	{
		if (c = 'a' && c = 'z')
		{
			return (0);
		}
		else
		{
			return (1);
		}
	}
}
